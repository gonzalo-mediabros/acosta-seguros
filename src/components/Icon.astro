---
interface Props {
  name: string;
  class?: string;
}

const { name, class: className } = Astro.props;
---

<span class={`material-symbols-outlined ${className || ""}`}>
  {name}
</span>
